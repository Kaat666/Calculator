first select operation
select operating mode(1 with the numbers , 2 with vectors )
select first number or size vectors 
select second number or vectors 
select choose whether you want to restart the program
